{
  "name": "full-stack-architecture-demo",
  "version": "1.0.0",
  "description": "CSC 436 Week 4 React + Axios + ASP.NET Core + EF Core demo",
  "scripts": {
    "dev": "concurrently \"dotnet run --project api --urls http://localhost:5000\" \"vite --host 127.0.0.1\"",
    "api": "dotnet run --project api --urls http://localhost:5000",
    "client": "vite --host 127.0.0.1",
    "build": "vite build",
    "start": "npm run dev"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^8.2.2",
    "vite": "^5.4.0"
  }
}